class Opacity {
  const Opacity._();
  
  static const Opacity _instance = Opacity._(); 

  static Opacity get instance => _instance;

  double get opacityMedium => 0.10;
  double get opacityHight => 0.50;
}